<%= form_for(@mexican) do |f| %>
  <% if @mexican.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mexican.errors.count, "error") %> prohibited this mexican from being saved:</h2>

      <ul>
      <% @mexican.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
