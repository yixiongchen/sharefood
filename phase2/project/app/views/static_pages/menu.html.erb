	<% provide(:title, 'SHARE') %>

<div class="center jumbotron">
  <h1>Welcome</h1>

  <h2>
    Make your choice!
  </h2>

  <%= link_to("Share food with others",{:controller =>'microposts',:action => 'new'},class: "btn btn-lg btn-primary")%>
  or
  <%= link_to("Search for Food",'/microposts/',class: "btn btn-lg btn-primary")%>

</div>

<head>
  <style type="text/css">
    body { height: 100%; margin: 0; padding: 0; }
    #map { height: 100%; }
  </style>
</head>
  
<body>
  
  <div id="map" style="height:350px;"></div>

  
  

  <script>
    initMap();
    drawMap(<%= raw @location.to_json %>, <%= raw @user_name.to_json %>, <%= raw @user_id.to_json %>, <%= raw @content.to_json %>, <%= raw @foodtype.to_json %>);
  </script>


</body>